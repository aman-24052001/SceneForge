# Image handling
Pillow
numpy

# CLI
typer

# Testing
pytest

# --- External binaries required (NOT pip-installable, see docs/architecture.md) ---
# colmap      -- apt-get install colmap  (CPU build works fine)
# opensplat   -- build from source: https://github.com/pierotofy/OpenSplat
#                CPU build needs: OpenCV dev + libtorch (CPU build)
